# ⚙️ McManus Orchestration Log

**Date:** 2026-03-26T21:05:45Z  
**Agent:** McManus  
**Issue:** #967 — Stack Overflow in Recursive Schema Traversal

## Completed Work

### 1. MSBuild Test Harness
- Created `test\MSBuild\petstore-preserve-original.refitter` (PreserveOriginal config)
- Normalized `test\MSBuild\petstore.refitter` format
- MSBuild task automatically discovers both .refitter files during build

### 2. Smoke Tests Enhancement
- Added PreserveOriginal variant to `test\smoke-tests.ps1:193`
  - Tested across all 13+ OpenAPI specifications
  - Validates CLI flag works with diverse schemas

### 3. GitHub Actions MSBuild Workflow
- Updated `.github\workflows\msbuild.yml` to include separate PreserveOriginal build step
- Split single build into two labeled steps:
  1. Default naming (original petstore.refitter)
  2. PreserveOriginal naming (new petstore-preserve-original.refitter)

### 4. Format Blocker Investigation & Fix
- **Problem:** `dotnet format --verify-no-changes` hung on Hockney's new `.refitter` files
- **Root Cause:** Design-time project loading in formatter workspace triggered recursive schema processing timeout
- **Solution:** Exclude `PropertyNamingPolicy.refitter` from `DesignTimeBuild` in `src\Refitter.SourceGenerator.Tests\Refitter.SourceGenerator.Tests.csproj`
  - Keeps regression coverage active for normal builds/tests
  - Avoids formatter-only hang path
  - Change confined to test harness configuration

### 5. Files Modified
- Created: `test\MSBuild\petstore-preserve-original.refitter`
- Modified: `test\MSBuild\petstore.refitter` — formatting normalization
- Modified: `.github\workflows\msbuild.yml` — separate PreserveOriginal step
- Modified: `test\smoke-tests.ps1` — added PreserveOriginal variant
- Modified: `src\Refitter.SourceGenerator.Tests\Refitter.SourceGenerator.Tests.csproj` — DesignTimeBuild exclusion

## Validation Status

✅ **Build:** `dotnet build -c Release src\Refitter.slnx` — PASS  
✅ **Tests:** `dotnet test -c Release src\Refitter.slnx` — PASS  
✅ **Format:** `dotnet format --verify-no-changes src\Refitter.slnx` — PASS

## Notes

- Format fix applied after Keaton's review feedback
- Surgical changes; MSBuild task logic untouched
- PreserveOriginal now under automated validation across three surfaces (MSBuild, CLI, SourceGenerator)
